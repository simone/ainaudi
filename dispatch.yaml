dispatch:
  # PDF generation (heavy, 1GB RAM, scale-to-zero)
  - url: "*/api/deleghe/processi/*"
    service: pdf

  # Django Admin
  - url: "*/admin/*"
    service: api

  # Campagna Open Graph
  - url: "*/campagna/*"
    service: api

  # API backend
  - url: "*/api/*"
    service: api

  # Tutto il resto → React frontend
  - url: "*/*"
    service: default

