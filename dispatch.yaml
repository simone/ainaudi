dispatch:
  # Django Admin
  - url: "*/admin/*"
    service: api

  # Campagna Open Graph (HTML con meta tags per social sharing)
  - url: "*/campagna/*"
    service: api

  # API backend
  - url: "*/api/*"
    service: api

  # Tutto il resto → React frontend
  - url: "*/*"
    service: default

