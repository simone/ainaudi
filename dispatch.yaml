dispatch:
  # Django Admin
  - url: "*/admin/*"
    service: api

  # Campagna Open Graph
  - url: "*/campagna/*"
    service: api

  # API backend
  - url: "*/api/*"
    service: api

  # Tutto il resto → React frontend
  - url: "*/*"
    service: default

